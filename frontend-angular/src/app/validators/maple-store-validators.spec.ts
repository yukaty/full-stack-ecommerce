import { MapleStoreValidators } from './maple-store-validators';

describe('MapleStoreValidators', () => {
  it('should create an instance', () => {
    expect(new MapleStoreValidators()).toBeTruthy();
  });
});
